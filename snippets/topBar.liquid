<article class="slider">
    <div class="slider__line">
        {% for i in (1..10) %}
            <div class="slider__item">
                <img src="/assets/starIcon-alt.svg" alt="Star Icon">
                <span>{{ settings['topBar'].settings.slider_text }}</span>
            </div>
        {% endfor %}
        {% for i in (1..10) %}
            <div class="slider__item">
                <img src="/assets/starIcon-alt.svg" alt="Star Icon">
                <span>{{ settings['topBar'].settings.slider_text }}</span>
            </div>
        {% endfor %}
    </div>
</article>